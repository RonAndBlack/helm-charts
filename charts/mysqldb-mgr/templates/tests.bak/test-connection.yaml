apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysqldb-mgr.fullname" . }}-test-connection"
  labels:
    {{- include "mysqldb-mgr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysqldb-mgr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
